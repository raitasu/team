import { TableHeader } from 'widgets/Table/TableHeader';

export const Home = () => (
  <TableHeader
    title="Employees"
    onAddButtonClick={() => ({})}
    onFilterButtonClick={() => ({})}
    isAdmin={false}
  />
);
